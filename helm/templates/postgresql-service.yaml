{{- if .Values.postgresql.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "liftosaur2garmin.postgresqlName" . }}
  labels:
    {{- include "liftosaur2garmin.labels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
spec:
  clusterIP: None
  ports:
    - name: postgresql
      port: {{ .Values.postgresql.service.port }}
      targetPort: postgresql
      protocol: TCP
  selector:
    {{- include "liftosaur2garmin.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: postgresql
{{- end }}
